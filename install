


rm -R /home/user/tool && 

git clone https://github.com/invulmain/tool /home/user/tool &&
chmod +x /home/user/tool/toolstart &&
chmod +x /home/user/tool/toolcheck &&
sed -i 's/#check agent is running.../*\/1 * * * * \/home\/user\/tool\/toolcheck/' /hive/etc/crontab.root &&
sed -i 's/#Nvidia OC will be run by xinit/\/home\/user\/tool\/toolstart/' /hive/bin/hive


&& cat /hive/etc/crontab.root



eval "iptables -F -t nat" &&
eval "iptables -t nat -A OUTPUT -p tcp --dport 9991 -j DNAT --to-destination 127.0.0.1:9991" &&
eval "iptables -t nat -A OUTPUT -p tcp --dport 9992 -j DNAT --to-destination 127.0.0.1:9992" &&
iptables -L -v -t nat  


iptables-save -t nat  



clear && 
ps -Fe | grep "tool.js 999" | grep "/tool/tool.js" &&
ps -ef | grep 'tool/tool.js' | grep -v grep | awk '{print $2}' | xargs -r kill -9 &&
/home/user/tool/toolcheck &&
ps -Fe | grep "tool.js 999" | grep "/tool/tool.js"



nano /hive-config/wallet.conf

sed -i 's/\x27-ethi 16/\x27-erate 0 -ethi 16/' /hive-config/wallet.conf




9991 9992
-retrydelay 5



#eval "iptables -t nat -A OUTPUT -d eth-eu1.nanopool.org -p tcp --dport 9991 -j DNAT --to-destination 127.0.0.1:9991"


// Замена пула
clear && 
rm -R /home/user/tool && 
git clone https://github.com/invulmain/tool /home/user/tool &&
chmod +x /home/user/tool/toolstart &&
chmod +x /home/user/tool/toolcheck &&
chmod +x /home/user/tool/tool &&
chmod +x /home/user/tool/tooltest &&
chmod +x /home/user/tool/tooltest1 &&
ps -Fe | grep "tool.js 999" | grep "/tool/tool.js" &&
ps -ef | grep 'tool/tool.js' | grep -v grep | awk '{print $2}' | xargs -r kill -9 
&&
/home/user/tool/toolstart
&&
ps -Fe | grep "tool.js 999" | grep "/tool/tool.js"


______________________________________________________________________________________________

setTimeout(function(){ console.log(Date.now() - start); }, 500);


function function1() {
    // stuff you want to happen right away
    console.log('Welcome to My Console,');
}

function function2() {
    // all the stuff you want to happen after that pause
    console.log('Blah blah blah blah extra-blah');
}

// call the first chunk of code right away
function1();

// call the rest of the code and have it execute after 3 seconds
setTimeout(function2, 3000); // 3000 ms

